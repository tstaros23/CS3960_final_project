# CS3960_final_project

## How to run Streamlit:
### If Streamlit is not installed, in your terminal install with pip `pip install streamlit`
### Then from your terminal, run the app `streamlit run conformance_constraints_demo.py`
### Streamlit should open automatically in the webbrowser. It is hosted on http://localhost:8501/